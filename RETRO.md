# 一页复盘 — Park OS 多维监控大屏

## 关键 AI Prompt 记录

### 第一轮：需求分析与方案设计
```
帮我检查这个项目，看满足以下条件不，不满足的话帮我完成，定位：
实现多维监控大屏与实时 IoT 数据流推送、智能故障诊断。
- 三类图表：自适应折线图、饼图与热力图
- 实时推送：部署 WebSocket 实时通道
```
→ AI 先通读全部源码，识别出 6 项缺失，进入 Plan Mode 出方案，用户审批后逐项实现。

### 第二轮：代码清理
```
你觉得哪些是垃圾代码就可以把一些代码删掉
```
→ AI 通过 grep 交叉验证 CSS 类名在 JS 中的引用，精准识别 40 行死代码（旧 CSS 柱状图、地图占位、空页面样式、未引用常量）。

### 第三轮：交付物补齐
```
满足要求吗？交付物：可运行前后端系统 + README + 一页复盘
```
→ AI 自查交付清单，补写 README 和本文档。

## 过程踩坑记录

| 坑 | 现象 | 解决 |
|----|------|------|
| **ECharts 实例泄漏** | 切换 tab 后图表不渲染或出现残影 | 增加 `chartInstances` 管理池，`disposeAllCharts()` 在切页时销毁，`notMerge: true` 全量替换数据 |
| **WebSocket 数据覆盖不完整** | 前端初始硬编码数据与 WebSocket 推送字段名不一致 | 统一前后端数据契约：`stats`/`energyTrend`/`env`/`deviceTypeStats`/`alerts`/`diagnosis` |
| **深色大屏与浅色面板冲突** | 大屏 CSS 变量泄漏到其他 tab 页面 | 通过 `.dashboard-screen` 作用域隔离，大屏样式不依赖 `:root` 变量 |
| **热力图 tooltip 显示归一化值** | tooltip 展示的是 0-100 归一化值而非原始物理量 | 保留原始 `heatData`，tooltip formatter 回查真实值显示物理单位 |
| **端口残留** | `EADDRINUSE` 错误 | 增加 `SIGINT` 优雅退出处理，`lsof -ti :3001 \| xargs kill` 清理 |
| **Vite CDN 加载顺序** | ECharts 未定义 | CDN script 放 `<head>`（阻塞加载），保证早于 `<script type="module">` 执行 |

## 后续优化方向

### 短期（可直接做）
- **告警问答交互**：对接 LLM API，点击告警自动生成深度分析报告
- **数据持久化**：服务端接入 SQLite，记录历史数据支持回放
- **更多图表类型**：雷达图（综合评分）、仪表盘（实时数值）
- **暗色/亮色主题切换**：目前大屏固定深色，其他页面固定浅色

### 中期（需要一定工作量）
- **真实 IoT 协议对接**：MQTT/CoAP 替代模拟数据
- **多园区切换**：支持多个园区实例，Tab 切换
- **告警规则配置化**：诊断规则从代码中抽离为 JSON 配置文件
- **E2E 测试**：Playwright 验证大屏渲染 + WebSocket 数据流

### 长期（架构演进）
- **微前端拆分**：大屏 / 设备管理 / 告警中心独立子应用
- **3D 数字孪生**：Three.js / Cesium 实现园区 3D 可视化
- **边缘计算**：诊断逻辑下沉到边缘网关，减少中心服务压力
